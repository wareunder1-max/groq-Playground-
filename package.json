{
  "name": "speech-to-text-app",
  "version": "1.0.0",
  "description": "A simple web application that captures audio and converts it to text using Groq's speech-to-text API",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "proxy": "node proxy-server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@vitest/ui": "^1.0.0",
    "fast-check": "^3.15.0",
    "jsdom": "^23.0.0",
    "vitest": "^1.0.0"
  }
}
